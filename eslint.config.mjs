// ESLint is intentionally disabled for this project.
// This config tells ESLint to ignore all files so that no linting is performed.

const eslintConfig = [
  {
    ignores: ["**/*"],
  },
];

export default eslintConfig;
